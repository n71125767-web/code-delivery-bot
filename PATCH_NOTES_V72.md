# V72 Main/FAQ Buttons + Number Callback Fix

- Fixed product card buttons `📱 Номер` and `⚙️ Выдача`: `v34:` callbacks are now routed to the admin handler.
- `📱 Номер` now sets product fulfillment to number/quantity and returns to the product card with stock hint.
- Admin menu button is shown on buyer main keyboard only for admins; it is no longer duplicated inside the admin keyboard.
- Buyer reply keyboard layout:
  - `🛒 Товары`
  - `🛍 Корзина` / `💼 Кошелёк`
  - `🧾 Мои заказы`
  - `📕 FAQ` / `🤝 Стать партнёром`
  - role switch buttons only when applicable.
- Settings now support:
  - main page text;
  - main page photo;
  - main page URL buttons;
  - FAQ text;
  - FAQ photo;
  - FAQ URL buttons;
  - custom reply keyboard buttons with URL response.
- After editing main/FAQ text/photo/buttons the admin stays in settings instead of being thrown to the main panel.
- `/start` in normal chat uses configured main page photo/buttons when configured.
