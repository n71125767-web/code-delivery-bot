services:
  - type: web
    name: code-delivery-bot
    runtime: python
    buildCommand: python -m pip install --no-cache-dir -r requirements.txt
    startCommand: python -m app.bot
    healthCheckPath: /health
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: ADMIN_IDS
        sync: false
      - key: DATABASE_URL
        sync: false
      - key: CRYPTO_PAY_TOKEN
        sync: false
      - key: CRYPTO_PAY_NETWORK
        value: testnet
      - key: CRYPTO_PAY_INVOICE_EXPIRES_SECONDS
        value: "3600"
      - key: CRYPTO_PAY_RECOVERY_INTERVAL_SECONDS
        value: "300"
      - key: STOCK_RESERVATION_TTL_SECONDS
        value: "3900"
      - key: PROXYLINE_ENABLED
        value: "0"
      - key: PROXYLINE_API_KEY
        sync: false
      - key: ALLOW_SQLITE_ON_RENDER
        value: "0"
